---
- name: Scale test replicaset to two
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/env python"
  tasks:
    - kubevirt_scale_vmirs:
        verify_ssl: no
        replicas: 2
        name: test-vmirs
        namespace: default
