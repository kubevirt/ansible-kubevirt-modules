---
- name: Scale freyja replicaset to three
  hosts: localhost
  connection: local
  tasks:
    - kubevirt_scale_vmirs:
        verify_ssl: no
        replicas: 1
        name: freyja
        namespace: vms
