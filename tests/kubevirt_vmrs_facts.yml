---
- name: Gather facts for freyja VirtualMachineReplicaSet
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/env python"
  tasks:
    - kubevirt_vmrs_facts:
        name: freyja
        namespace: vms
    - debug:
        var: kubevirt_vmrs
