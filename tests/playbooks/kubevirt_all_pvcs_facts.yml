---
- name: Gather facts for all PersistentVolumeClaim on default namespace
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/env python"
  tasks:
    - kubevirt_facts:
        namespace: default
        kind: PersistentVolumeClaim
      register: pvc_facts
    - debug:
        var: pvc_facts
