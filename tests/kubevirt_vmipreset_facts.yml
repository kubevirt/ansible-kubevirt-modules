---
- name: Gather facts for jhendrix VirtualMachine
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/env python"
  tasks:
    - kubevirt_vmpreset_facts:
        name: vmps-small
        namespace: vms
    - debug:
        var: kubevirt_vmpreset
