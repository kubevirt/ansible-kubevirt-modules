---
- name: Gather facts for PersistentVolumeClaim of vms namespace
  hosts: localhost
  connection: local
  vars:
    ansible_python_interpreter: "/usr/bin/env python"
  tasks:
    - kubevirt_facts:
        kind: PersistentVolumeClaim
        namespace: vms
      register: pvc_facts
    - debug:
        var: pvc_facts
