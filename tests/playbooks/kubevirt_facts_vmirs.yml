---
- name: Gather facts for test VirtualMachineInstanceReplicaSet
  hosts: localhost
  connection: local
  tasks:
    - k8s_facts:
        name: test-vmirs
        namespace: default
        kind: VirtualMachineInstanceReplicaSet
      register: test_vmirs
    - debug:
        var: test_vmirs
